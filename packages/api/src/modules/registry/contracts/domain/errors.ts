import { RegistryCredentialErrors } from '@quantum/contracts/modules/registry/errors';
import { defineErrors } from '@/shared/errors/defineErrors';

export const RegistryCredentialError = defineErrors(RegistryCredentialErrors);
